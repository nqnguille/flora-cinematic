---
import Layout from '../../layouts/Layout.astro'
import '../../styles/global.css'

const googleClientId = import.meta.env.PUBLIC_GOOGLE_CLIENT_ID || ''
---

<Layout
  title="Carta de genéticas — solo socios | Flora ONG"
  description="Stock de genéticas disponible, exclusivo para socios verificados de Flora."
  noindex={true}
>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet" />
  <main class="gn-main">
    <!-- ── Login ── -->
    <div id="gn-login" class="gn-login">
      <div class="gn-login-card">
        <div class="gn-brand">
          <svg class="gn-brand-leaf" viewBox="0 0 32 32" fill="none" aria-hidden="true"><path d="M16 6C16 6 10 10 10 17C10 20.3137 12.6863 23 16 23C19.3137 23 22 20.3137 22 17C22 10 16 6 16 6Z" fill="currentColor"/><line x1="16" y1="22" x2="16" y2="27" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          <span class="gn-brand-name">Flora <em>club</em></span>
        </div>
        <h1 class="gn-title">Carta de <em>genéticas</em></h1>
        <p class="gn-sub">
          Sección exclusiva para socios verificados de Flora. Ingresá con la cuenta de
          Google que registraste como socio.
        </p>
        <div id="g_id_button"></div>
        <p id="gn-login-msg" class="gn-msg" hidden></p>
        <a href="/" class="gn-back">← Volver al sitio</a>
      </div>
    </div>

    <!-- ── Carta (fullscreen · carrusel) ── -->
    <div id="gn-content" class="gn-content" hidden>
      <div class="gn-topbar">
        <img class="gn-logo" src="/img/flora-logo-white.png" alt="Flora" width="1472" height="298" />
        <div class="gn-filters">
          <div id="gn-dd-tipo" class="gn-dd" data-dim="tipo"></div>
          <div id="gn-dd-formato" class="gn-dd" data-dim="formato"></div>
          <div id="gn-dd-efecto" class="gn-dd" data-dim="efecto"></div>
          <div id="gn-dd-sabor" class="gn-dd" data-dim="sabor"></div>
        </div>
        <div class="gn-topbar-right">
        <div class="gn-ctrls" role="group" aria-label="Control del carrusel">
          <button id="gn-prev" class="gn-ctrl" type="button" aria-label="Anterior"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 5-7 7 7 7"/></svg></button>
          <button id="gn-playpause" class="gn-ctrl gn-ctrl-pp" type="button" aria-label="Pausar"><svg class="ic-pause" viewBox="0 0 24 24" fill="currentColor"><rect x="6.5" y="5" width="3.7" height="14" rx="1"/><rect x="13.8" y="5" width="3.7" height="14" rx="1"/></svg><svg class="ic-play" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5.2v13.6l10.5-6.8z"/></svg></button>
          <button id="gn-next" class="gn-ctrl" type="button" aria-label="Siguiente"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 5 7 7-7 7"/></svg></button>
          <button id="gn-fs" class="gn-ctrl gn-ctrl-fs" type="button" aria-label="Pantalla completa"><svg class="ic-expand" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 9V5a1 1 0 0 1 1-1h4M20 9V5a1 1 0 0 0-1-1h-4M4 15v4a1 1 0 0 0 1 1h4M20 15v4a1 1 0 0 0-1 1h-4"/></svg><svg class="ic-compress" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 4v3a2 2 0 0 1-2 2H4M15 4v3a2 2 0 0 0 2 2h3M9 20v-3a2 2 0 0 0-2-2H4M15 20v-3a2 2 0 0 1 2-2h3"/></svg></button>
        </div>
        <button id="gn-logout" class="gn-logout" type="button" aria-label="Cerrar sesión">Salir</button>
        </div>
      </div>

      <div id="gn-grid" class="gn-track" aria-label="Carrusel de genéticas"></div>

      <footer class="gn-pagefoot">
        <p class="gn-disclaimer">Acceso exclusivo para pacientes vinculados a <strong>Asociación Civil Cann de Cannabis Medicinal</strong>, comprendido en el marco de la Ley 27.350 y el programa REPROCANN.</p>
      </footer>
    </div>
  </main>

  <script src="https://accounts.google.com/gsi/client" async defer is:inline></script>
  <script src="/socios/icons.js" is:inline></script>
  <script src="/socios/bancos.js" is:inline></script>
  <script define:vars={{ googleClientId }}>
    function escapeHtml(s) {
      return String(s ?? '').replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]))
    }

    const TIPO_LABEL = { sativa: 'Sativa', indica: 'Indica', hibrida: 'Híbrida', cbd: 'Full CBD' }
    const LEAF_SVG = '<svg viewBox="0 0 32 32" fill="none" aria-hidden="true"><path d="M16 6C16 6 10 10 10 17C10 20.3137 12.6863 23 16 23C19.3137 23 22 20.3137 22 17C22 10 16 6 16 6Z" fill="currentColor"/><line x1="16" y1="22" x2="16" y2="27" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>'

    // Formatos: FLORES = hoja de cannabis (Tabler, MIT) · PREROLL = cono con brasa
    // sólida y corte del papel a la altura del filtro (abajo).
    const ICON_FLOR = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M7 20s0 -2 1 -3.5c-1.5 0 -2 -.5 -4 -1.5c0 0 1.839 -1.38 5 -1c-1.789 -.97 -3.279 -2.03 -5 -6c0 0 3.98 -.3 6.5 3.5c-2.284 -4.9 1.5 -9.5 1.5 -9.5c2.734 5.47 2.389 7.5 1.5 9.5c2.531 -3.77 6.5 -3.5 6.5 -3.5c-1.721 3.97 -3.211 5.03 -5 6c3.161 -.38 5 1 5 1c-2 1 -2.5 1.5 -4 1.5c1 1.5 1 3.5 1 3.5c-2 0 -4.438 -2.22 -5 -3c-.563 .78 -3 3 -5 3"/><path d="M12 22v-5"/></svg>'
    const ICON_PREROLL = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M10.8 20.9 9.5 8.7Q9.4 7.7 10.2 7.3L13.8 7.3Q14.6 7.7 14.5 8.7L13.2 20.9Q13.1 21.6 12 21.6Q10.9 21.6 10.8 20.9Z"/><path d="M10.6 17.6Q12 16.9 13.4 17.6"/><path d="M10.2 7.3Q12 5.3 13.8 7.3" fill="currentColor"/></svg>'
    const FORMATO = {
      flor: { label: 'Flores', svg: ICON_FLOR },
      preroll: { label: 'Preroll', svg: ICON_PREROLL },
    }

    // Formatos de una genética; por compatibilidad, sin dato = ['flor'].
    function formatosDe(g) {
      return Array.isArray(g.formatos) && g.formatos.length ? g.formatos : ['flor']
    }

    let allGeneticas = []
    // Cada dimensión es un Set: multi-selección (OR dentro de la dimensión, AND entre dimensiones).
    const filters = { q: '', tipo: new Set(), formato: new Set(), efecto: new Set(), sabor: new Set() }

    /* ── Helpers de datos ── */

    // "70% Sativa | 30% Indica" → { sativa: 70, indica: 30 } (null si no se puede leer)
    function parsePredominancia(pred) {
      if (!pred) return null
      const s = /(\d+)\s*%\s*sativa/i.exec(pred)
      const i = /(\d+)\s*%\s*indica/i.exec(pred)
      if (!s && !i) return null
      const sat = s ? Number(s[1]) : 100 - Number(i[1])
      return { sativa: sat, indica: 100 - sat }
    }

    // "25% THC" → 25 (null si no es un porcentaje legible, ej. "1:1")
    function parsePct(v) {
      const m = /([\d.]+)\s*%/.exec(String(v ?? ''))
      return m ? Number(m[1]) : null
    }

    /* ── Piezas de la tarjeta ── */

    // Predominancia genética sin números: "Sativa predominante | Silver River"
    // Entre 40 y 60% sativa se considera equilibrada.
    function predGenetica(g) {
      const p = parsePredominancia(g.predominancia)
      if (p) {
        if (p.sativa > 60) return 'Sativa predominante'
        if (p.sativa < 40) return 'Índica predominante'
        return 'Híbrida equilibrada'
      }
      return TIPO_LABEL[g.tipo] || g.tipo || ''
    }

    function infoLine(g) {
      const gen = `<span class="gn-gen">${escapeHtml(predGenetica(g))}</span>`
      const banco = g.banco ? `<p class="gn-bank-line">${escapeHtml(g.banco)}</p>` : ''
      return `<p class="gn-info">${gen}</p>${banco}<span class="gn-rule"></span>`
    }

    // Medidor de cannabinoides: barra sin números. Sin dato → slot "—"
    // para que todas las tarjetas conserven la misma geometría.
    // IMPORTANTE: THC y CBD comparten la misma escala dentro de la tarjeta,
    // así los anchos relativos son comparables entre sí.
    function meter(label, rawValue, scaleMax) {
      const pct = parsePct(rawValue)
      if (pct === null) {
        return `<div class="gn-meter"><span class="gn-meter-label">${label}</span><span class="gn-meter-raw">—</span></div>`
      }
      const w = Math.min(100, Math.max(3, (pct / scaleMax) * 100))
      return `
        <div class="gn-meter">
          <span class="gn-meter-label">${label}</span>
          <div class="gn-meter-track"><span class="gn-meter-fill" style="width:${w}%"><i class="gn-meter-knob"></i></span></div>
        </div>`
    }

    // Ítems de círculo (icono + etiqueta) de una categoría. La etiqueta sale del
    // texto libre ("Mermelada cítrica | Skittlez de uva") si la cantidad coincide
    // con los iconos; si no, se usa la etiqueta genérica del icono.
    function circleItems(category, keys, texto) {
      if (!keys || !keys.length) return []
      const segments = String(texto || '')
        .split(/[|·]/)
        .map((s) => s.trim())
        .filter(Boolean)
      const useSegments = segments.length === keys.length
      return keys
        .map((key, idx) => {
          const def = (window.FLORA_TAG_ICONS[category] || {})[key]
          if (!def) return ''
          const label = useSegments ? segments[idx] : def.label
          return `
            <div class="gn-circle-item">
              <span class="gn-circle">${def.svg}</span>
              <span class="gn-circle-label">${escapeHtml(label)}</span>
            </div>`
        })
        .filter(Boolean)
    }

    // Un grupo (efecto o sabor) con su etiqueta de referencia y sus círculos.
    function circleGroup(labelText, items) {
      if (!items.length) return ''
      return `<div class="gn-circle-group"><span class="gn-circle-grouplabel">${labelText}</span><div class="gn-circle-cluster">${items.join('')}</div></div>`
    }

    // Los 4 iconos en una fila: efectos | sabores, con separador en el medio.
    function circleStrip(g) {
      const groups = [
        circleGroup('Efectos', circleItems('efecto', g.efectos, g.efectoTexto)),
        circleGroup('Sabores', circleItems('sabor', g.sabores, g.saborTexto)),
      ].filter(Boolean)
      if (!groups.length) return ''
      return `<div class="gn-circle-row">${groups.join('<span class="gn-circle-sep"></span>')}</div>`
    }

    function card(g, idx) {
      const tipo = ['sativa', 'indica', 'hibrida', 'cbd'].includes(g.tipo) ? g.tipo : 'hibrida'
      // Foto por convención: /socios/geneticas/<id>.webp — si falla, la tarjeta
      // cae al placeholder de marca (la hoja) manteniendo la geometría. `g.foto`
      // (URL cargada desde el admin) tiene prioridad.
      const fotoSrc = g.foto || (g.id ? `/socios/geneticas/${g.id}.webp` : '')
      const delay = Math.min(idx || 0, 8) * 45
      // Escala compartida THC/CBD: 0-30 salvo que ambos sean bajos (0-5)
      const vThc = parsePct(g.thc) ?? 0
      const vCbd = parsePct(g.cbd) ?? 0
      const scaleMax = Math.max(vThc, vCbd) <= 5 ? 5 : 30
      // Sellos circulares: Flores (izquierda) y Preroll (derecha), solo icono.
      const seals = formatosDe(g)
        .map((f) => FORMATO[f] ? `<span class="gn-seal gn-seal-${f}" title="${FORMATO[f].label}" aria-label="${FORMATO[f].label}">${FORMATO[f].svg}</span>` : '')
        .join('')
      return `
        <article class="gn-item" data-tipo="${tipo}" style="--d:${delay}ms">
          <div class="gn-item-hero${fotoSrc ? '' : ' is-nophoto'}">
            ${fotoSrc ? `<img class="gn-item-img" src="${escapeHtml(fotoSrc)}" alt="${escapeHtml(g.nombre)}" loading="lazy" onerror="this.remove();this.closest('.gn-item-hero').classList.add('is-nophoto')" />` : ''}
            <span class="gn-item-watermark">${LEAF_SVG}</span>
            ${seals}
            <h3 class="gn-item-name">${escapeHtml(g.nombre)}</h3>
          </div>
          <div class="gn-item-body">
            <div class="gn-head">${infoLine(g)}</div>
            <div class="gn-meters">
              ${meter('THC', g.thc, scaleMax)}
              ${meter('CBD', g.cbd, scaleMax)}
            </div>
            ${circleStrip(g)}
          </div>
        </article>`
    }

    /* ── Filtros ── */

    function matches(g) {
      if (filters.tipo.size && !filters.tipo.has(g.tipo)) return false
      if (filters.formato.size && !formatosDe(g).some((f) => filters.formato.has(f))) return false
      if (filters.efecto.size && !(g.efectos || []).some((e) => filters.efecto.has(e))) return false
      if (filters.sabor.size && !(g.sabores || []).some((s) => filters.sabor.has(s))) return false
      if (filters.q) {
        const hay = `${g.nombre} ${g.banco || ''}`.toLowerCase()
        if (!hay.includes(filters.q)) return false
      }
      return true
    }

    /* ── Carrusel: auto-deslizante + controles (pausa / adelantar / rebobinar) ── */
    const SPEED = 0.55 // px por frame
    let loopOffset = 0   // ancho de una copia (para el loop sin costura)
    let singleCount = 0  // tarjetas de UNA copia
    let pendingRecenter = false
    let userPaused = false   // pausa explícita (botón)
    let hoverPaused = false  // pausa temporal (mouse encima / arrastrando)
    let touchPaused = false  // pausa temporal (touch/wheel/prev/next reciente)
    let resumeTimer = null
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches

    function measure() {
      const track = document.getElementById('gn-grid')
      const secondCopyStart = track.children[singleCount]
      loopOffset = secondCopyStart ? secondCopyStart.offsetLeft : 0
      // Arrancamos en la 2ª copia para poder rebobinar sin toparse con el borde.
      if (pendingRecenter && loopOffset > 0) { track.scrollLeft = loopOffset; pendingRecenter = false }
      fitNames(track)
    }

    // Tamaño del nombre según el ANCHO REAL de la card (no del viewport): grande y
    // protagonista en cualquier pantalla, con la palabra más larga entrando en 1 línea.
    function fitNames(track) {
      const first = track.querySelector('.gn-item')
      if (!first) return
      const w = first.getBoundingClientRect().width || first.clientWidth
      if (!w) { requestAnimationFrame(() => fitNames(track)); return } // aún sin layout: reintentar
      // 6.98 = ancho de la palabra más larga (WATERMELON) por px de fuente → entra justa.
      const fs = (w - 22) / 6.98
      track.querySelectorAll('.gn-item-name').forEach((el) => { el.style.fontSize = fs + 'px' })
    }

    function pauseTouchSoon(delay) {
      touchPaused = true
      clearTimeout(resumeTimer)
      resumeTimer = setTimeout(() => { touchPaused = false }, delay || 4000)
    }

    function stepAmount(track) {
      const item = track.querySelector('.gn-item')
      const w = item ? item.getBoundingClientRect().width : 320
      return w + 20
    }

    let carouselStarted = false
    function startCarousel() {
      if (carouselStarted) return
      carouselStarted = true
      const track = document.getElementById('gn-grid')
      const playpause = document.getElementById('gn-playpause')

      track.addEventListener('mouseenter', () => { hoverPaused = true })
      track.addEventListener('mouseleave', () => { hoverPaused = false })
      track.addEventListener('touchstart', () => { touchPaused = true; clearTimeout(resumeTimer) }, { passive: true })
      track.addEventListener('touchend', () => pauseTouchSoon(4000), { passive: true })
      track.addEventListener('wheel', () => pauseTouchSoon(4000), { passive: true })
      window.addEventListener('resize', () => requestAnimationFrame(measure))

      // Arrastrar con el mouse para mover el carrusel a gusto
      let dragging = false, lastX = 0
      track.addEventListener('pointerdown', (e) => {
        if (e.pointerType !== 'mouse') return
        dragging = true; lastX = e.clientX; track.classList.add('is-grabbing')
      })
      window.addEventListener('pointermove', (e) => {
        if (!dragging) return
        track.scrollLeft -= (e.clientX - lastX); lastX = e.clientX
      })
      window.addEventListener('pointerup', () => {
        if (!dragging) return
        dragging = false; track.classList.remove('is-grabbing')
      })

      // Adelantar / rebobinar de a una tarjeta
      document.getElementById('gn-prev').addEventListener('click', () => {
        track.scrollLeft -= stepAmount(track); pauseTouchSoon(4000)
      })
      document.getElementById('gn-next').addEventListener('click', () => {
        track.scrollLeft += stepAmount(track); pauseTouchSoon(4000)
      })
      // Pausa / reproducir explícito
      playpause.addEventListener('click', () => {
        userPaused = !userPaused
        playpause.classList.toggle('is-paused', userPaused)
        playpause.setAttribute('aria-label', userPaused ? 'Reproducir' : 'Pausar')
      })

      // Pantalla completa
      const fsBtn = document.getElementById('gn-fs')
      fsBtn.addEventListener('click', () => {
        if (document.fullscreenElement) document.exitFullscreen()
        else document.documentElement.requestFullscreen().catch(() => {})
      })
      document.addEventListener('fullscreenchange', () => {
        const on = !!document.fullscreenElement
        fsBtn.classList.toggle('is-fs', on)
        fsBtn.setAttribute('aria-label', on ? 'Salir de pantalla completa' : 'Pantalla completa')
      })

      function frame() {
        if (loopOffset > 0) {
          if (!userPaused && !hoverPaused && !touchPaused && !reduceMotion) {
            track.scrollLeft += SPEED
          }
          // Loop infinito en ambos sentidos: mantener en [loopOffset, 2·loopOffset)
          if (track.scrollLeft < loopOffset) track.scrollLeft += loopOffset
          else if (track.scrollLeft >= loopOffset * 2) track.scrollLeft -= loopOffset
        }
        requestAnimationFrame(frame)
      }
      requestAnimationFrame(frame)
    }

    function applyFilters() {
      const list = allGeneticas.filter(matches)
      const track = document.getElementById('gn-grid')

      if (!list.length) {
        singleCount = 0
        loopOffset = 0
        track.scrollLeft = 0
        track.innerHTML = `<p class="gn-empty">${allGeneticas.length === 0 ? 'No hay genéticas cargadas por el momento.' : 'Ninguna genética coincide con esos filtros.'}</p>`
      } else {
        // Repetimos el listado con buffer a ambos lados para loop sin huecos y
        // poder rebobinar; el corte se hace al ancho de UNA copia.
        const copies = Math.max(3, Math.ceil((window.innerWidth * 2.4) / (list.length * 320)) + 1)
        let html = ''
        for (let c = 0; c < copies; c++) html += list.map((g, i) => card(g, i)).join('')
        track.innerHTML = html
        singleCount = list.length
        pendingRecenter = true
        requestAnimationFrame(measure)
      }
    }

    /* ── Desplegables tildables (multi-selección con checkboxes) ── */
    const DIM_LABEL = { tipo: 'Tipo', formato: 'Formato', efecto: 'Efectos', sabor: 'Sabores' }
    const STATIC_OPTS = {
      tipo: [['sativa', 'Sativa'], ['indica', 'Indica'], ['hibrida', 'Híbrida'], ['cbd', 'Full CBD']],
      formato: [['flor', 'Flores'], ['preroll', 'Prerolls']],
    }

    function closeAllPanels(except) {
      document.querySelectorAll('.gn-dd-panel').forEach((p) => {
        if (p !== except) {
          p.hidden = true
          const t = p.previousElementSibling
          if (t) t.classList.remove('is-open')
        }
      })
    }
    document.addEventListener('click', () => closeAllPanels(null))

    function updateDdCount(dim) {
      const el = document.getElementById('gn-dd-' + dim)
      const n = filters[dim].size
      const c = el.querySelector('.gn-dd-count')
      if (n === 0) { c.textContent = 'Todos'; c.classList.add('is-all') }
      else { c.textContent = n; c.classList.remove('is-all') }
      el.querySelector('.gn-dd-toggle').classList.toggle('has-sel', n > 0)
    }

    function renderDropdown(dim, options) {
      const el = document.getElementById('gn-dd-' + dim)
      if (!options.length) { el.hidden = true; return }
      el.hidden = false
      const allChecked = filters[dim].size === 0 ? 'checked' : ''
      const allRow = `<label class="gn-dd-opt gn-dd-all"><input type="checkbox" class="gn-dd-allcb" ${allChecked} /><span class="gn-dd-check"></span>Todos</label><div class="gn-dd-divider"></div>`
      const opts = options
        .map(([val, lab]) => {
          const checked = filters[dim].has(val) ? 'checked' : ''
          return `<label class="gn-dd-opt"><input type="checkbox" class="gn-dd-cb" value="${escapeHtml(val)}" ${checked} /><span class="gn-dd-check"></span>${escapeHtml(lab)}</label>`
        })
        .join('')
      el.innerHTML = `
        <button class="gn-dd-toggle" type="button">
          <span class="gn-dd-name">${DIM_LABEL[dim]}</span>
          <span class="gn-dd-count"></span>
          <svg class="gn-dd-chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
        </button>
        <div class="gn-dd-panel" hidden>${allRow}${opts}</div>`
      const toggle = el.querySelector('.gn-dd-toggle')
      const panel = el.querySelector('.gn-dd-panel')
      const allcb = panel.querySelector('.gn-dd-allcb')
      toggle.addEventListener('click', (e) => {
        e.stopPropagation()
        const willOpen = panel.hidden
        closeAllPanels(willOpen ? panel : null)
        panel.hidden = !willOpen
        toggle.classList.toggle('is-open', willOpen)
      })
      panel.addEventListener('click', (e) => e.stopPropagation())

      // "Todos" = sin filtro. Tocarlo limpia la dimensión y destilda lo específico.
      allcb.addEventListener('change', () => {
        filters[dim].clear()
        panel.querySelectorAll('.gn-dd-cb').forEach((c) => { c.checked = false })
        allcb.checked = true
        updateDdCount(dim)
        applyFilters()
      })

      panel.querySelectorAll('.gn-dd-cb').forEach((cb) => {
        cb.addEventListener('change', () => {
          if (cb.checked) filters[dim].add(cb.value)
          else filters[dim].delete(cb.value)
          allcb.checked = filters[dim].size === 0
          updateDdCount(dim)
          applyFilters()
        })
      })
      updateDdCount(dim)
    }

    function buildFilters() {
      renderDropdown('tipo', STATIC_OPTS.tipo)
      renderDropdown('formato', STATIC_OPTS.formato)
      const present = { efecto: new Set(), sabor: new Set() }
      for (const g of allGeneticas) {
        for (const e of g.efectos || []) present.efecto.add(e)
        for (const s of g.sabores || []) present.sabor.add(s)
      }
      const toOpts = (cat, set) =>
        [...set].sort().map((k) => [k, (window.FLORA_TAG_ICONS[cat] || {})[k]?.label || k])
      renderDropdown('efecto', toOpts('efecto', present.efecto))
      renderDropdown('sabor', toOpts('sabor', present.sabor))
    }

    /* ── Sesión ── */

    function showContent(list) {
      allGeneticas = list
      document.getElementById('gn-login').hidden = true
      document.getElementById('gn-content').hidden = false
      buildFilters()
      applyFilters()
      startCarousel()
    }

    function showLoginError(message) {
      const msg = document.getElementById('gn-login-msg')
      msg.textContent = message
      msg.hidden = false
    }

    async function loadGeneticas(email) {
      const res = await fetch('/api/socios/geneticas', { credentials: 'include' })
      if (!res.ok) return false
      const data = await res.json()
      showContent(data.geneticas || [])
      return true
    }

    window.handleGoogleCredential = async (response) => {
      try {
        const res = await fetch('/api/socios/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ credential: response.credential }),
        })
        const data = await res.json()
        if (!res.ok || !data.ok) {
          showLoginError(data.error || 'No se pudo verificar tu cuenta.')
          return
        }
        await loadGeneticas(data.email)
      } catch (err) {
        showLoginError('Error de red al verificar con Google.')
      }
    }

    document.getElementById('gn-logout').addEventListener('click', async () => {
      await fetch('/api/socios/logout', { method: 'POST', credentials: 'include' })
      location.reload()
    })

    if (googleClientId) {
      window.onload = () => {
        google.accounts.id.initialize({
          client_id: googleClientId,
          callback: window.handleGoogleCredential,
        })
        google.accounts.id.renderButton(document.getElementById('g_id_button'), {
          theme: 'filled_black',
          size: 'large',
          text: 'signin_with',
        })
      }
    } else {
      showLoginError('Falta configurar el Google Client ID del sitio.')
    }

    loadGeneticas()
  </script>
</Layout>

<style is:global>
  /* ═══════════════════════════════════════════════
     CARTA DE GENÉTICAS — Flora de noche
     Violeta de marca al mando · verde bosque para datos
     Fondo violeta profundo (#130d1c) · texto crema
     Cormorant (serif de marca) en títulos y nombres · sin neón
  ═══════════════════════════════════════════════ */

  /* Fondo de la página al mismo violeta oscuro (evita el borde blanco) */
  html, body { background-color: #130d1c; }

  .gn-main {
    --gn-bg:      #130d1c;  /* violeta casi negro (deriva del #381F56) */
    --gn-card:    #1c1430;
    --gn-card-2:  #251a3d;
    --gn-line:    rgba(244, 241, 247, 0.1);
    --gn-cream:   #f4f1f7;  /* texto principal */
    --gn-muted:   rgba(244, 241, 247, 0.55);
    --gn-violet:  #b79de6;  /* violeta de marca aclarado — voz de marca */
    --gn-green:   #3cb492;  /* verde bosque (#0A503C) aclarado — dato vivo */
    --gn-green-d: #0a503c;

    min-height: 100svh;
    background: var(--gn-bg);
    font-family: var(--font-ui);
    color: var(--gn-cream);
    position: relative;
  }

  /* Atmósfera de marca, muy sutil */
  .gn-main::before {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(55% 40% at 85% 0%, rgba(56, 31, 86, 0.45), transparent 70%),
      radial-gradient(50% 35% at 5% 100%, rgba(10, 80, 60, 0.14), transparent 70%);
    z-index: 0;
  }
  .gn-main > * { position: relative; z-index: 1; }

  /* ── Marca ── */
  .gn-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--gn-violet);
  }
  .gn-brand-leaf { width: 30px; height: 30px; }
  .gn-brand-name {
    font-size: 1.15rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--gn-cream);
  }
  .gn-brand-name em {
    font-style: normal;
    color: var(--gn-violet);
    font-weight: 600;
  }

  .gn-tagline {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.2rem;
    color: var(--gn-violet);
    text-align: right;
  }

  /* Título de página en Cormorant, con palabra-acento en verde itálica */
  .gn-title {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(2.4rem, 5.5vw, 3.8rem);
    line-height: 1;
    letter-spacing: 0.005em;
    margin: 0;
    color: var(--gn-cream);
  }
  .gn-title em {
    font-style: italic;
    color: var(--gn-green);
  }

  .gn-back {
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--gn-muted);
    text-decoration: none;
  }
  .gn-back:hover { color: var(--gn-cream); }

  /* ── Login ── */
  .gn-login {
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-m) var(--space-s);
  }
  .gn-login[hidden] { display: none; }

  .gn-login-card {
    width: 100%;
    max-width: 500px;
    background: var(--gn-card);
    border: 1px solid var(--gn-line);
    border-radius: 22px;
    padding: clamp(30px, 4vw, 48px);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    box-shadow: 0 24px 60px -30px rgba(0, 0, 0, 0.7);
  }
  .gn-login-card .gn-title { font-size: clamp(2rem, 5vw, 2.9rem); }

  .gn-sub {
    line-height: 1.6;
    color: var(--gn-muted);
    font-size: 0.95rem;
    margin: 0 0 0.4rem;
  }

  .gn-msg {
    color: var(--gn-violet);
    font-size: 0.9rem;
  }

  /* ── Carta (fullscreen · carrusel) ── */
  .gn-content {
    height: 100svh;
    display: flex;
    flex-direction: column;
    gap: clamp(8px, 1.1vh, 16px);
    padding: clamp(12px, 1.6vh, 22px) clamp(14px, 2vw, 30px);
    overflow: hidden;
  }
  .gn-content[hidden] { display: none; }

  /* Barra superior slim */
  .gn-bar {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    gap: 0.6rem clamp(1rem, 2vw, 2rem);
    flex-wrap: wrap;
  }
  .gn-bar .gn-title {
    font-size: clamp(1.5rem, 2.6vw, 2.4rem);
    line-height: 1;
    flex: 1 1 auto;
  }
  .gn-bar-right {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    min-width: 0;
  }
  .gn-count { font-size: 0.8rem; color: var(--gn-muted); white-space: nowrap; }
  .gn-count strong { font-size: 1.05rem; font-weight: 800; color: var(--gn-green); margin-right: 0.12rem; }
  .gn-welcome {
    font-size: 0.75rem; color: var(--gn-muted);
    max-width: 180px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  }
  .gn-logout {
    background: none; border: 1px solid var(--gn-line); border-radius: 999px;
    padding: 0.32rem 0.85rem; font-family: var(--font-ui); font-size: 0.76rem; font-weight: 600;
    cursor: pointer; color: var(--gn-cream); white-space: nowrap; transition: border-color 0.15s ease;
  }
  .gn-logout:hover { border-color: var(--gn-violet); }

  /* Filtros centrados en una sola línea */
  /* Filtros dentro del topbar (en el medio, una sola línea) */
  .gn-filters {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    flex-wrap: nowrap;
    flex: 1 1 auto;
    min-width: 0;
  }

  /* Barra superior derecha: controles discretos + Salir */
  .gn-topbar {
    flex: 0 0 auto;
    z-index: 25;
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: clamp(8px, 1.2vh, 16px);
  }
  .gn-logo { height: clamp(24px, 2.7vh, 36px); width: auto; display: block; }
  .gn-topbar-right { display: flex; align-items: center; gap: 0.6rem; }

  .gn-search-wrap { position: relative; flex: 0 0 auto; width: clamp(150px, 20vw, 240px); }
  .gn-search-wrap svg {
    position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
    width: 15px; height: 15px; color: var(--gn-muted); pointer-events: none;
  }
  .gn-search {
    width: 100%; font-family: var(--font-ui); font-size: 0.82rem;
    padding: 0.48rem 0.8rem 0.48rem 2.15rem;
    border: 1px solid var(--gn-line); border-radius: 999px;
    background: var(--gn-card); color: var(--gn-cream);
  }
  .gn-search::placeholder { color: rgba(244, 241, 247, 0.35); }
  .gn-search:focus { outline: none; border-color: var(--gn-violet); }

  /* Desplegables tildables (multi-selección) */
  .gn-dd { position: relative; flex: 0 0 auto; }
  .gn-dd[hidden] { display: none; }
  .gn-dd-toggle {
    display: inline-flex; align-items: center; gap: 0.45rem;
    font-family: var(--font-ui); font-size: 0.82rem; font-weight: 600;
    color: var(--gn-cream); background: var(--gn-card);
    border: 1px solid var(--gn-line); border-radius: 999px;
    padding: 0.48rem 0.95rem; cursor: pointer;
    transition: border-color 0.15s ease;
  }
  .gn-dd-toggle:hover { border-color: rgba(244, 241, 247, 0.3); }
  .gn-dd-toggle.has-sel { border-color: var(--gn-violet); }
  .gn-dd-count {
    display: inline-flex; align-items: center; justify-content: center;
    min-width: 1.15rem; height: 1.15rem; padding: 0 0.35rem;
    font-size: 0.66rem; font-weight: 800; border-radius: 999px;
    background: var(--gn-violet); color: #130d1c;
  }
  /* Estado "Todos": texto tenue, sin pastilla */
  .gn-dd-count.is-all {
    min-width: auto; height: auto; padding: 0;
    background: none; color: var(--gn-muted);
    font-size: 0.78rem; font-weight: 600;
  }
  .gn-dd-divider { height: 1px; background: var(--gn-line); margin: 0.35rem 0.3rem; }
  .gn-dd-all { font-weight: 700; }
  .gn-dd-chev { width: 12px; height: 12px; color: var(--gn-violet); transition: transform 0.18s ease; }
  .gn-dd-toggle.is-open .gn-dd-chev { transform: rotate(180deg); }

  .gn-dd-panel {
    position: absolute; top: calc(100% + 6px); left: 0; z-index: 30;
    min-width: 190px; max-height: min(58vh, 380px); overflow-y: auto;
    background: var(--gn-card-2); border: 1px solid var(--gn-line);
    border-radius: 14px; padding: 0.4rem;
    box-shadow: 0 20px 48px -14px rgba(0, 0, 0, 0.75);
  }
  .gn-dd-panel[hidden] { display: none; }
  .gn-dd-opt {
    display: flex; align-items: center; gap: 0.65rem;
    padding: 0.45rem 0.6rem; border-radius: 9px; cursor: pointer;
    font-size: 0.86rem; color: var(--gn-cream);
    transition: background 0.12s ease;
  }
  .gn-dd-opt:hover { background: rgba(244, 241, 247, 0.06); }
  .gn-dd-opt input { position: absolute; opacity: 0; width: 0; height: 0; }
  .gn-dd-check {
    width: 18px; height: 18px; border-radius: 6px; flex-shrink: 0;
    border: 1.6px solid rgba(244, 241, 247, 0.3);
    display: inline-flex; align-items: center; justify-content: center;
    transition: background 0.12s ease, border-color 0.12s ease;
  }
  .gn-dd-opt input:checked + .gn-dd-check { background: var(--gn-violet); border-color: var(--gn-violet); }
  .gn-dd-opt input:checked + .gn-dd-check::after {
    content: ''; width: 5px; height: 9px;
    border: solid #130d1c; border-width: 0 2px 2px 0;
    transform: rotate(45deg) translate(-0.5px, -1px);
  }
  .gn-dd-opt input:focus-visible + .gn-dd-check { outline: 2px solid var(--gn-violet); outline-offset: 2px; }

  /* ── Sellos de formato sobre la foto (círculo sólido, solo icono) ── */
  .gn-seal {
    position: absolute;
    top: 0.8rem;
    z-index: 1;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    box-shadow:
      0 6px 18px -4px rgba(0, 0, 0, 0.7),
      inset 0 0 0 1.8px rgba(255, 255, 255, 0.32);
  }
  .gn-seal svg { width: 35px; height: 35px; }
  .gn-seal-flor { left: 0.8rem; background: var(--gn-green); color: #08160f; }
  .gn-seal-preroll { right: 0.8rem; background: var(--gn-violet); color: #170c2b; }

  /* ── Carrusel horizontal (marquesina) ── */
  .gn-track {
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    align-items: stretch;
    gap: clamp(0.9rem, 1.4vw, 1.4rem);
    overflow-x: auto;
    overflow-y: hidden;
    scrollbar-width: none;
  }
  .gn-track::-webkit-scrollbar { display: none; }
  .gn-track { cursor: grab; }
  .gn-track.is-grabbing { cursor: grabbing; user-select: none; }

  /* ── Controles del carrusel: discretos, arriba a la derecha ── */
  .gn-ctrls {
    display: flex;
    align-items: center;
    gap: 0.1rem;
  }
  .gn-ctrl {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    border-radius: 50%;
    border: none;
    background: none;
    color: var(--gn-muted);
    cursor: pointer;
    transition: color 0.15s ease, background 0.15s ease, transform 0.08s ease;
  }
  .gn-ctrl:hover { color: var(--gn-cream); background: rgba(244, 241, 247, 0.08); }
  .gn-ctrl:active { transform: scale(0.9); }
  .gn-ctrl svg { width: 16px; height: 16px; }
  .gn-ctrl-pp { color: var(--gn-cream); }
  .gn-ctrl-pp .ic-play { display: none; }
  .gn-ctrl-pp.is-paused .ic-play { display: block; }
  .gn-ctrl-pp.is-paused .ic-pause { display: none; }
  .gn-ctrl-fs .ic-compress { display: none; }
  .gn-ctrl-fs.is-fs .ic-compress { display: block; }
  .gn-ctrl-fs.is-fs .ic-expand { display: none; }
  .gn-ctrl:focus-visible { outline: 2px solid var(--gn-violet); outline-offset: 2px; }

  .gn-item {
    flex: 0 0 auto;
    width: clamp(340px, 27vw, 540px);
    height: 100%;
    display: flex;
    flex-direction: column;
    background: var(--gn-card);
    border: 1px solid var(--gn-line);
    border-radius: 18px;
    overflow: hidden;
    transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
  }
  .gn-item:hover {
    border-color: rgba(183, 157, 230, 0.4);
    box-shadow: 0 18px 44px -18px rgba(0, 0, 0, 0.85);
  }

  /* Hero: foto con el nombre montado sobre el degradé (mini-póster) */
  .gn-item-hero {
    position: relative;
    /* Altura FIJA e idéntica en todas las tarjetas: depende del viewport, no
       del contenido. El nombre está anclado al fondo del hero, así que con el
       hero de alto constante el nombre queda SIEMPRE a la misma Y en todas las
       tarjetas, sin importar cuánto mida el body. */
    flex: 0 0 auto;
    height: clamp(300px, 46vh, 540px);
    overflow: hidden;
    background: var(--gn-card-2);
  }
  .gn-item-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.33, 1, 0.68, 1);
  }
  .gn-item:hover .gn-item-img { transform: scale(1.05); }
  .gn-item-hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg,
      transparent 32%,
      rgba(19, 13, 28, 0.35) 55%,
      rgba(19, 13, 28, 0.82) 80%,
      var(--gn-card) 100%);
  }
  /* Placeholder de marca cuando no hay foto */
  .gn-item-hero.is-nophoto {
    background: linear-gradient(150deg, #2a1f47 0%, #17251d 100%);
  }
  .gn-item-watermark {
    display: none;
    position: absolute;
    right: -6px;
    top: 50%;
    transform: translateY(-60%);
    width: 120px;
    height: 120px;
    color: var(--gn-cream);
    opacity: 0.08;
  }
  .gn-item-hero.is-nophoto .gn-item-watermark { display: block; }
  .gn-item-watermark svg { width: 100%; height: 100%; }

  .gn-item-name {
    position: absolute;
    left: 0.75rem;
    right: 0.75rem;
    bottom: 0.6rem;
    z-index: 1;
    font-family: 'Bebas Neue', var(--font-ui);
    font-weight: 400;
    /* Grande y protagonista. Tope ~3.5rem: la palabra más larga (WATERMELON)
       entra en una línea en la card de 450; 2+ palabras wrapean por palabra. */
    font-size: clamp(2.4rem, 3vw, 3.5rem);
    line-height: 0.9;
    letter-spacing: 0.005em;
    text-transform: uppercase;
    margin: 0;
    color: var(--gn-cream);
    text-shadow: 0 2px 22px rgba(0, 0, 0, 0.6);
  }

  .gn-item-body {
    display: flex;
    flex-direction: column;
    /* El body absorbe TODO el sobrante de la tarjeta (queda debajo del hero
       fijo). El contenido se apoya arriba; el excedente queda como aire abajo,
       con el color de la tarjeta, sin costura con el degradé del hero. */
    flex: 1 1 auto;
    min-height: 0;
    overflow: hidden;
    /* Reparte encabezado · THC/CBD · iconos en todo el alto: sin hueco abajo. */
    justify-content: space-between;
    padding: clamp(1rem, 1.6vw, 1.4rem);
  }
  .gn-head { flex: 0 0 auto; }

  /* Renglón 1: predominancia / tipo · Renglón 2: banco */
  .gn-info {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.35rem 0.45rem;
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    margin: 0;
  }
  .gn-info .gn-gen { color: var(--gn-violet); }
  .gn-info i { font-style: normal; color: var(--gn-violet); opacity: 0.7; }
  .gn-bank-line {
    margin: 0.3rem 0 0;
    font-size: 0.86rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--gn-green);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Separador full-width: divide el "encabezado" (predominancia + banco) de la
     ficha técnica (medidores + iconos) que va debajo. */
  .gn-rule {
    display: block;
    width: 100%;
    height: 1px;
    background: var(--gn-line);
    margin: 0.85rem 0 1rem;
  }

  /* ── Medidores THC / CBD (lado a lado) ── */
  /* ── Medidores de cannabinoides (barras sin números) ── */
  .gn-meters {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem 1.3rem;
    margin-bottom: 1.1rem;
  }
  .gn-meter-label {
    display: block;
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--gn-violet);
    margin-bottom: 0.5rem;
    white-space: nowrap;
  }
  .gn-meter-raw { color: var(--gn-muted); font-weight: 700; line-height: 1; }
  .gn-meter-track {
    height: 6px;
    border-radius: 999px;
    background: rgba(244, 241, 247, 0.14);
    overflow: visible;
  }
  .gn-meter-fill {
    position: relative;
    display: block;
    height: 100%;
    border-radius: 999px;
    background: linear-gradient(90deg, var(--gn-violet), var(--gn-green));
    animation: gn-fill 0.7s cubic-bezier(0.33, 1, 0.68, 1) 0.25s both;
  }
  @keyframes gn-fill { from { width: 0; } }
  .gn-meter-knob {
    position: absolute;
    right: -7px;
    top: 50%;
    transform: translateY(-50%);
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background: #fff;
    box-shadow: 0 1px 6px rgba(0, 0, 0, 0.55);
  }

  /* ── Secciones con círculos ── */
  .gn-section { margin-bottom: 1.05rem; }
  .gn-section:last-child { margin-bottom: 0.2rem; }

  .gn-section-title {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--gn-violet);
    margin: 0 0 0.8rem;
  }
  .gn-section-title::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--gn-line);
  }

  .gn-circle-row {
    display: flex;
    align-items: stretch;
    gap: 0.7rem;
    margin-top: 0.4rem;
  }
  .gn-circle-group {
    flex: 1 1 0;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }
  .gn-circle-grouplabel {
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--gn-muted);
    text-align: center;
  }
  .gn-circle-cluster {
    display: flex;
    justify-content: space-evenly;
    gap: 0.4rem;
    width: 100%;
  }
  .gn-circle-sep {
    width: 1px;
    background: var(--gn-line);
    align-self: stretch;
    flex-shrink: 0;
  }
  .gn-circle-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    flex: 0 1 auto;
    min-width: 0;
    max-width: 130px;
    text-align: center;
  }
  .gn-circle {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    border: 1.5px solid rgba(244, 241, 247, 0.16);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gn-green);
    flex-shrink: 0;
  }
  .gn-circle svg { width: 28px; height: 28px; }
  .gn-circle-label {
    font-size: 0.66rem;
    font-weight: 600;
    color: var(--gn-cream);
    opacity: 0.85;
    line-height: 1.2;
    width: 100%;
    /* Reserva SIEMPRE 2 líneas y recorta con puntos suspensivos: el alto es
       constante haya 1 o 2 palabras ("Cítrico" vs "Mermelada cítrica"), y un
       texto largo nunca dispara una 3ª línea que rompa la geometría. */
    min-height: calc(0.66rem * 1.2 * 2);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    overflow-wrap: break-word;
    word-break: break-word;
    hyphens: auto;
  }

  /* ── Footer slim ── */
  .gn-foot {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.9rem;
    color: var(--gn-violet);
    opacity: 0.8;
  }
  .gn-foot-text {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    white-space: nowrap;
  }
  .gn-foot-leaf { width: 26px; height: 26px; flex-shrink: 0; color: var(--gn-green); }

  .gn-empty { color: var(--gn-muted); }

  /* ── Footer de página ── */
  .gn-pagefoot {
    flex: 0 0 auto;
    display: flex;
    justify-content: center;
    padding-top: clamp(8px, 1.2vh, 16px);
    margin-top: clamp(6px, 1vh, 12px);
    border-top: 1px solid var(--gn-line);
  }
  .gn-disclaimer {
    max-width: 920px;
    margin: 0 auto;
    text-align: center;
    font-size: 0.72rem;
    line-height: 1.55;
    letter-spacing: 0.01em;
    color: var(--gn-muted);
  }
  .gn-disclaimer strong { color: var(--gn-cream); font-weight: 600; }

  /* Foco por teclado con identidad de marca */
  .gn-sel:focus-visible,
  .gn-search:focus-visible,
  .gn-logout:focus-visible,
  .gn-back:focus-visible {
    outline: 2px solid var(--gn-violet);
    outline-offset: 2px;
  }

  @media (prefers-reduced-motion: reduce) {
    .gn-meter-fill, .gn-item-img { animation: none !important; transition: none !important; }
  }

  @media (max-width: 640px) {
    .gn-filters { padding: 2.4rem 0.5rem 0; }
    .gn-item { width: 80vw; }
    .gn-circle { width: 50px; height: 50px; }
    .gn-circle svg { width: 22px; height: 22px; }
  }
</style>
